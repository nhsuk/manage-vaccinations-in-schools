#!/bin/bash -e

# If running the rails server then create or migrate existing database
if [ "${*}" == "./bin/rails server" ] || [ "${*}" == "./bin/bundle exec good_job start" ]; then
  ./bin/rails db:prepare:ignore_concurrent_migration_exceptions
fi

exec "${@}"

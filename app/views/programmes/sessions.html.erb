<%= content_for :page_title, "#{@programme.name} – School sessions" %>

<% content_for :before_main do %>
  <%= render AppBreadcrumbComponent.new(items: [
                                          { text: t("programmes.index.title"), href: programmes_path },
                                          { text: @programme.name, href: programme_path(@programme) },
                                        ]) %>
<% end %>

<h1 class="nhsuk-heading-l"><%= @programme.name %></h1>

<%= render AppProgrammeNavigationComponent.new(@programme, active: :sessions) %>

<% [[@scheduled_sessions, "scheduled session"],
    [@completed_sessions, "completed session"]]
     .select { |sessions, _| sessions.any? }.each do |sessions, description| %>

  <%= render AppSessionTableComponent.new(sessions, description:, show_dates: true, show_consent_period: true) %>
<% end %>

<%= render "imports/header" %>

<%= render AppImportsNavigationComponent.new(active: @active, team: current_team) %>

<%= render AppImportsTableComponent.new(
      team: current_team,
      pagy: @pagy,
      uploaded_files: uploaded_files_view?,
      mixnmatch_imports: @mixnmatch_imports,
    ) %>
<%= render AppPaginationComponent.new(pagy: @pagy) %>

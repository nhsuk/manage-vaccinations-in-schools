<% legend = "Select a team" %>

<% content_for :page_title, legend %>

<%= form_with model: @form, url: users_teams_path do |f| %>
  <%= f.govuk_error_summary %>

  <%= f.govuk_collection_radio_buttons :team_id,
                                       @form.teams,
                                       :id,
                                       :name,
                                       legend: { text: legend, size: "xl", tag: "h1" } %>

  <%= f.govuk_submit %>
<% end %>

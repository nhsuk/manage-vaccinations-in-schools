<div class="govuk-grid-row">
  <div class="govuk-grid-column-full">
    <%
    patient_details = @patient_details.select { |p, a, o| filter.call(@session, p, a, o) }
    if patient_details.any?
    %>
    <table id="patients" class="nhsuk-table">
      <thead class="govuk-table__head">
        <tr class="govuk-table__row">
          <th class="govuk-table__header">Name</th>
          <th class="govuk-table__header">Triage reason</th>
          <th class="govuk-table__header">Action</th>
        </tr>
      </thead>
      <tbody class="govuk-table__body">
        <% patient_details.each do |patient, action, outcome| %>
          <%= render "patient_row", middle_column: :triage_reasons, patient:, action:, outcome: %>
        <% end %>
      </tbody>
    </table>
<% else %>
    <%= render "layouts/empty_list", message: "We couldn’t find any children that matched your filters." %>
<% end %>
  </div>
</div>

<% content_for :before_main do %>
  <%= govuk_back_link(href: @back_link_path) %>
<% end %>

<% legend = "Which programmes will you run in this session?" %>
<% content_for :page_title, legend %>

<%= form_with model: @draft_session, url: wizard_path, method: :put do |f| %>
  <% content_for(:before_content) { f.govuk_error_summary } %>

  <%= f.govuk_collection_check_boxes :programme_types, @programmes, :type, :name,
                                     legend: { text: legend, size: "l", tag: "h1" },
                                     caption: { text: @draft_session.location&.name, size: "l" } %>

  <%= f.govuk_submit "Continue" %>
<% end %>

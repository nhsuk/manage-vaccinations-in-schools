<% content_for :before_main do %>
  <%= render AppBacklinkComponent.new(
    href: url_for(action: :start),
    name: "start page",
  ) %>
<% end %>

<%= h1 "Check and confirm answers", class: "nhsuk-heading-l" %>

<%= govuk_button_to "Confirm",
                    url_for(action: :record),
                    method: :put %>
